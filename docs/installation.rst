============
Installation
============

At the command line::

    $ easy_install django-flexible-parameters

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-flexible-parameters
    $ pip install django-flexible-parameters
